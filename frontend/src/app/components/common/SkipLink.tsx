/** Visually hidden until focused — jumps to #main-content on public/booking pages. */
export function SkipLink() {
  return (
    <a href="#main-content" className="skip-link">
      Skip to main content
    </a>
  );
}
